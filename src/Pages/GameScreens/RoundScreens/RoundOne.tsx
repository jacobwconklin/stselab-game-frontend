import './RoundOne.scss';

// RoundOne
// Force all players to only select one professional golfer as their solver for the whole hole
// for the whole course. This is to show what the traditional approach is of not breaking the 
// problem down beforehand. It also should show that the professional does well when they handle
// an entire hole. 
const RoundOne = (props: any) => {

    return (
        <div className='RoundOne'>

        </div>
    )
}

export default RoundOne;