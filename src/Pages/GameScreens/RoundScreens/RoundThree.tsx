import './RoundThree.scss';

// RoundThree
// Have players play on lp_arch, so they choose one solver type for long range and 
// one solver type for close range hits (they can be the same). This will teach
// that breaking a problem apart can lead to better, or cheaper solutions than the 
// traditional handoff of the entire problem. 
const RoundThree = (props: any) => {

    return (
        <div className='RoundThree'>

        </div>
    )
}

export default RoundThree;