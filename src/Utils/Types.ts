// Temporary global types file to hold all types, will be further organized as data load increases.
export interface UserInformation {
    firstName: string;
    lastName: string;
    age: number;
    email: string;
    // May also want answers to some questions or results from some game like Yahtzee  
}